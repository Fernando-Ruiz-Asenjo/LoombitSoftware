fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
pydantic[email]>=2.8.0
httpx>=0.27.0
pillow>=10.0.0

# Skill W Loombit Pilot — control local del equipo
pynput>=1.7.6                              # ratón + teclado (control de escritorio)
psutil>=5.9.0                              # info de procesos para focus_window
# Solo Windows (importadas de forma perezosa; pip las omite en Linux/CI)
pywinauto>=0.6.8; sys_platform == "win32"  # UI Automation Windows (fallback a ctypes)
pywin32>=306; sys_platform == "win32"      # win32gui para foco de ventana (fallback a ctypes)

# Google Workspace (OAuth + Gmail + Calendar)
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.120.0

# Cifrado del token OAuth en reposo (clave en el almacén del SO)
cryptography>=42.0.0
keyring>=24.0.0

# Inteligencia documental — extracción de texto de PDFs de facturas
pypdf>=4.0.0

# Dev / calidad
pytest>=8.0.0
pytest-cov>=5.0.0
black>=24.0.0
ruff>=0.6.0
