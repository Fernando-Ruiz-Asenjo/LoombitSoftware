fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0
pydantic[email]>=2.8.0
httpx>=0.27.0
pillow>=10.0.0

# Skill W Loombit Pilot — control local del equipo
pynput>=1.7.6          # ratón + teclado (siempre requerido)
pywinauto>=0.6.8       # UI Automation Windows (opcional, fallback a ctypes)
psutil>=5.9.0          # info de procesos para focus_window
pywin32>=306           # win32gui para foco de ventana (fallback a ctypes)

# Google Workspace (OAuth + Gmail + Calendar)
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.120.0

# Dev / calidad
pytest>=8.0.0
pytest-cov>=5.0.0
black>=24.0.0
ruff>=0.6.0
