# Allowlist del CABLE (scripts/bridge_local.py --gate allowlist).
# Una expresión regular por línea. Un comando recibido de la nube SOLO se ejecuta
# si CASA con alguna de estas líneas. Líneas vacías y con '#' se ignoran.
#
# Empieza restrictivo: descomenta/añade solo lo que quieras permitir sin confirmar.
# Ejemplos seguros de lectura:
# ^git (status|log|fetch|diff|branch)\b
# ^python -m pytest\b
# ^python -m loombit_operator\.launcher\b
# ^(Get-Date|whoami|pwd|ls|dir)\b
# ^Invoke-RestMethod http://127\.0\.0\.1:8787/
