"""Tools auto-generadas y APROBADAS por la Fábrica de Skills (cuarentena).

Cada módulo lo escribe `fabrica/materializar.py` SOLO tras aprobación humana, y se re-verifica con
el gate de seguridad antes de importarse. El contenido es código auto-escrito (no editar a mano):
es producto de runtime, no se versiona (ver `.gitignore`).
"""
